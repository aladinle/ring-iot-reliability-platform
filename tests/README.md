# Tests

Top-level test organization for cross-stack validation.

```text
tests/
├── cpp/
└── backend/
```

Device-specific C++ tests also live under `device_simulator/tests` so they can be wired directly into CMake.

